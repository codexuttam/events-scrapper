# Example environment variables for event-scrapper
# Copy this file to .env and fill in real values for production.

# Flask / app
DB_PATH=sqlite:///events.db
BASE_URL=http://localhost:5000

# Admin credentials (used by the simple admin login endpoint)
# For security, set strong values in production.
ADMIN_USER=admin
ADMIN_PASS=changeme
# Alternatively you can set ADMIN_TOKEN to a single shared token.
# ADMIN_TOKEN=

# SMTP (optional). If provided, app will attempt to send confirmation emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@example.com

# Rate limiting (per-IP). Defaults are fine for local dev.
RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW=3600
ADMIN_SESSION_TTL=3600

# Frontend: set NEXT_PUBLIC_API_BASE when running Next.js dev server if API isn't on localhost:5000
# NEXT_PUBLIC_API_BASE=http://localhost:5000
